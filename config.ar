let url = env.get_value("URL","https://isotope.wbell.dev")
term.log("URL: "+url)
let domain = env.get_value("domain","isotope.wbell.dev")
term.log("domain: "+domain)
let port = number(env.get_value("PORT", 7030))
term.log("port: "+port)

let env_keys = env.keys()
for (i from 0 to env_keys.length) do
    term.log(env_keys[i]+": "+env[env_keys[i]])